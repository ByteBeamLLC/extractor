/**
 * App URL for the main application (app.parsli.co in production).
 * Used by marketing pages to link to sign-in, sign-up, dashboard, etc.
 */
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000"
